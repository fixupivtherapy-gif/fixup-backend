import './globals.css';

export const metadata = {
  title: "JD's Property Solutions",
  description:
    'Plataforma de trabajo para mayoristas e inversionistas en bienes raíces.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
